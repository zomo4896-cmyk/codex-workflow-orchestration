<!-- BEGIN:global-model-orchestration -->
# Global workflow-aware model orchestration

Resolve `<CODEX_HOME>` from CODEX_HOME, or the user home directory plus `.codex` when unset.
Read and follow `<CODEX_HOME>/skills/workflow-model-orchestration/SKILL.md` for every task. The user authorizes its bounded model-specific subagent delegation without repeated model-choice questions, subject to higher-priority instructions and available tools. Simple work stays with the coordinator. Use `<CODEX_HOME>/model-routing/models.json` for current assignments. This module is independent of the optional ADHD skills. Explicit user choices prevail; saved defaults do not change already-running models.
<!-- END:global-model-orchestration -->
